/**
 * Routes configurations for the whole application
 * @type Object
 */
export const routes = {
    dashboard: {
        path: '/',
    },
    plot: {
        path: '/plot/:id',
    },
    profile: {
      path: '/profile',
    },
    register: {
      path: '/register',
    },
};
